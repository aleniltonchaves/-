#Modos de manipular cadeia de caracteres - strings - na string 'Curso em Video' temos:
##Fatiamento - [] fatiamento [:]
##análise - len(frase) , frase.count('o') , frase.find('deo') , receber a posição -1 quer dizer "não encontrado".
# Operador 'in' analisa se há na string o que se pede.
#transformação - frase.replace('python','android') ; frase.upper() ; frase.lower() ; frase.capitalize() ; frase.title()
#frase.strip() remove espaços adcionais, seja right 'rstrip' ou left strip, lstrip.
#divisão - frase.split() - divide a string se baseando nos espaços. e a jução é feita com '-'.join(frase)